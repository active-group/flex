# Nginx frontend integration

## Containerized version

Note that `sites-enabled/default.conf` is generated from `default.template`.

## Stand-alone

```
nginx -p <flex>/erlbank/nginx -e <flex>/erlbank/nginx/logs/error.log
```
