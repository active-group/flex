# Starting the system

Deploy & start with `docker compose up --build`
or `docker compose up --build --force-recreate`

UI then shows up here:

http://localhost:8080/

Opensearch Dashboard (opensearch alternative of kibana):

http://localhost:5601/

- Login with
  - user: admin
  - password: admin

```
